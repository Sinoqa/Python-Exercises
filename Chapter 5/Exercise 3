#  Write a program that asks the user to enter a value n, and then computes (1+ 1/2 + 1/3 ...) -
# ln(n). The ln function is log in the math module
import math
n = int(input("Input n here > "))
number = 1
for i in range(2,n):
    number +=1/n
    
print(number-math.log(n))

