# Use a for loop to print a box like the one below. Allow the user to specify how wide and how
# high the box should be. A hollow box
height = int(input("Height of the box > "))
width = int(input("Width of the box > "))
print("*"*(width))
for i in range(1,height+1):
    space =" "*(width-2)
    print("*" + space + "*")
print("*"*(width))
